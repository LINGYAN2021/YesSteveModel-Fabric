# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project
.profileconfig.json

# idea
out
*.ipr
*.iws
*.iml
.idea

# gradle
build
.gradle
/local.properties

# native
/src/main/resources/META-INF/native/*
# native/ysm.dll 是随仓库分发的原生核心库（Windows x64），必须入库，
# 否则 clone 后无法构建（shadowJar 与内置模型索引生成都依赖它）。
# 重建原生模块后，用新产物覆盖该文件并提交。

# 构建期生成的资源，由 processResources 依赖的 generateBuiltinModelIndex 任务重建
/src/generated/

# other
eclipse
/run
/logs
__pycache__
/.codegraph

# Files from Forge MDK
forge*changelog.txt

# dark past
hs_err_pid*

package com.elfmcys.ysm.client.event;

/**
 * 第一人称背景（手臂后方模型）渲染。
 * 3.0 上游代码中实际渲染调用仍处于注释状态（等待原生渲染接口），
 * 因此移植版暂不注册任何处理器，仅保留类结构。
 */
public class RenderFirstPlayerBackground {
    public static void register() {
        // 暂无实现
    }
}
